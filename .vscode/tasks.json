{
    "tasks": [
        {
            "type": "shell",
            "label": "Build and Install",
            "command": "pebble build && pebble install --emulator gabbro && pebble logs --emulator gabbro",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "options": {
                "cwd": "${workspaceFolder}/blue_pixel"
            },
            "problemMatcher": []
        }
    ],
    "version": "2.0.0"
}